{
    "default": {
        "FEATURE": {
            "type": "string",
            "defaultValue": "None",
            "description": "The feature (branch) to select if it exists"
        },
        "BASE_BRANCH": {
            "type": "string",
            "defaultValue": "master",
            "description": "The branch on which to base the build"
        }
    },
    "additional": {
        "FULL_BUILD": {
            "type": "boolean", 
            "defaultValue": false, 
            "description": "Check to clean whole project before building. Otherwise it will only rebuild the applicative part of the firmware"
        },
        "PLATFORM": {
            "type": "string",
            "defaultValue": "None",
            "description": "The platform to be built"
        }
    }
}